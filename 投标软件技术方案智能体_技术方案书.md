# 投标软件技术方案智能体 · 技术方案书

| 项目名称 | 投标软件技术方案智能体（Agent 应用） |
|---|---|
| 文档版本 | V1.0 |
| 编制日期 | 2026-08-15 |
| 核心定位 | 技术方案撰写主力：招标文件 → 评分点解析 → 方案生成 → Word 产出 |
| 部署形态 | Web 应用（浏览器访问，多人协作） |
| 技术路线 | 开源框架自建（LangGraph / LlamaIndex + 大模型 API） |

---

## 一、项目概述

### 1.1 项目背景

投标活动是企业获取订单的核心渠道，而**技术方案**是投标文件中最重要、最耗时、最难写好的部分。当前企业投标普遍存在以下痛点：

1. **撰写周期长**：一份完整的技术方案动辄上百页，人工撰写需 3~7 天，且多轮修改反复；
2. **评分点遗漏**：招标文件中的评分项、废标条款、实质性要求繁多，人工逐条核对容易遗漏，直接导致失分甚至废标；
3. **公司素材难复用**：历史中标案例、产品手册、资质证书散落在个人电脑和文件夹中，检索困难、引用不规范；
4. **格式与合规要求高**：投标文件对章节结构、页码、目录、资质证明位置有严格格式要求，人工排版费时费力；
5. **经验依赖强**：方案质量高度依赖资深售前个人能力，难以标准化、规模化。

### 1.2 项目目标

建设一个「投标软件技术方案智能体」，实现：

- **输入**：招标文件（PDF/Word）+ 公司资料库（历史案例、产品手册、资质证书）；
- **输出**：符合招标评分标准的、可编辑的 Word 技术方案初稿 + 评分点对标分析报告；
- **核心价值**：将技术方案初稿产出时间从 **3~7 天缩短到 2~4 小时**，评分点覆盖率达到 **95% 以上**，历史素材复用率显著提升。

### 1.3 建设范围与边界

**范围内（本项目交付）**：

- 招标文件解析与评分点提取；
- 评分点对标分析（逐条需求 → 应答策略 → 得分预估）；
- 技术方案骨架生成与逐节内容撰写（RAG 增强）；
- 人工审阅、批注反馈与局部迭代重写；
- Word 技术方案文档导出（模板渲染）；
- 公司资料库的接入、入库、检索与管理；
- 多用户 Web 工作台（项目、会话、版本管理）。

**范围外（一期不做）**：

- 商务标、报价标、资信标等其他投标文件的自动生成；
- 投标全流程管理（如标讯跟踪、开标提醒、投后复盘）；
- 合同签订与项目交付环节。

### 1.4 用户角色

| 角色 | 职责 | 关键诉求 |
|---|---|---|
| 投标专员 | 发起投标任务、上传招标文件、导出标书 | 快速出稿、格式规范、操作简单 |
| 售前工程师 | 维护公司资料库、校准方案内容 | 素材好管理、内容准确可引用 |
| 方案审核人（技术负责人） | 批注审阅、把关技术内容与评分覆盖 | 对标清晰、批注高效、可追踪 |

---

## 二、需求分析

### 2.1 业务流程（端到端）

```
上传招标文件 → 智能解析（结构 + 评分点） → 资料库匹配
     → 生成方案骨架 → 逐节生成内容（人工可随时介入）
     → 评分对标报告 → 人工审阅批注 → 局部迭代重写
     → 导出 Word 标书章节 → 归档复用
```

关键设计：**全程可人工介入（HITL, Human-in-the-Loop）**，智能体不"黑盒"出稿，每一步产出用户都能查看、修改、反馈。

### 2.2 功能需求（FR）

**FR1 招标文件解析**
- 支持 PDF、Word（.doc/.docx）格式上传（一期），后续扩展扫描件 OCR；
- 自动识别招标文件章节结构（招标公告、投标人须知、评分办法、技术需求、合同条款等）；
- 提取**评分点**（评分项、分值、评审标准）、**实质性要求**（★条款、废标条款）、**资格要求**，输出结构化 JSON。

**FR2 评分点对标分析**
- 生成「评分点对照表」：条款号、评分项、分值、评审要点、我方应答策略、引用素材、得分预估；
- 按分值权重排序，突出高分项与易失分项，供撰写优先级决策。

**FR3 方案骨架生成**
- 依据评分点与招标技术需求，自动生成方案章节树（目录级大纲）；
- 骨架与评分点建立映射关系，保证「每个评分点都有对应章节应答」。

**FR4 章节内容生成（RAG 增强）**
- 按章节顺序逐节生成，每节自动检索公司资料库中的相关案例、产品、资质内容；
- 生成内容带**引用溯源**（来源文档、段落位置），杜绝凭空编造；
- 支持指定字数、语气风格（专业/保守）、重点倾向（如突出国产化、突出安全）等参数。

**FR5 人工审阅与迭代**
- 方案以章节为单位展示，支持行内批注、修改建议；
- 批注反馈后，智能体对**指定章节局部重写**（而非全文重写），保留已确认内容；
- 版本管理：每次迭代生成新版本，可回溯对比。

**FR6 Word 文档导出**
- 按公司标准标书模板渲染，输出格式规范的 .docx（目录、页码、标题层级、表格样式）；
- 支持导出评分对照表、资质索引等附表。

**FR7 公司资料库管理**
- 售前工程师上传/维护产品手册、历史中标方案、案例、资质证书、专利软著等；
- 自动分类、打标、建立元数据；支持全文检索与向量检索。

**FR8 历史项目复用**
- 相似招标项目自动匹配历史方案，支持"以旧方案为底稿 + 新标调整"模式；
- 沉淀"应答策略库"：高频评分点的最优应答话术。

### 2.3 非功能需求（NFR）

| 类别 | 要求 |
|---|---|
| 性能 | 方案初稿生成 ≤ 30 分钟（百页级）；单章节生成 ≤ 60 秒；检索响应 ≤ 3 秒 |
| 并发 | 支持 20 并发用户，10 个并行生成任务 |
| 可用性 | 核心服务可用性 ≥ 99.5%；生成任务支持断点恢复 |
| 安全 | 数据加密存储；权限隔离（角色级）；操作审计日志；支持私有化部署（可选） |
| 可扩展 | 模型可插拔（多厂商切换）；能力模块可扩展（后续加商务标等） |
| 合规 | 投标资料仅用于本项目用途；满足企业数据安全与保密要求 |

---

## 三、系统总体设计

### 3.1 设计原则

1. **Agent 优先，流程兜底**：核心链路用 Agent 编排（LangGraph 状态机），复杂场景允许模型自主决策，但关键节点设确定性兜底；
2. **人机协同**：所有生成产出可审阅、可介入、可回退；
3. **引用可溯源**：生成内容必须基于资料库检索结果，杜绝幻觉；
4. **模块解耦**：解析、检索、生成、导出均为独立服务，可单独升级替换；
5. **模型可插拔**：通过统一 LLM 网关适配 DeepSeek、Qwen、GLM、Kimi 等，按任务特性路由。

### 3.2 系统架构（分层视图）

```
┌─────────────────────────────────────────────────────────────┐
│ 用户层   投标专员          售前工程师          方案审核人       │
├─────────────────────────────────────────────────────────────┤
│ 应用层   Web 前端 · 投标工作台（上传/对话/批注/导出）          │
├─────────────────────────────────────────────────────────────┤
│ 智能体层 Agent 编排引擎（LangGraph）：任务分解/工具调度/       │
│          上下文管理/人工介入（HITL）                          │
├─────────────────────────────────────────────────────────────┤
│ 能力层   招标解析 │ 评分对标 │ RAG检索 │ 章节生成 │ 审阅迭代 │ 文档导出 │
├─────────────────────────────────────────────────────────────┤
│ 数据层   公司资料库 │ 向量库 │ 业务库 │ 文档存储              │
├─────────────────────────────────────────────────────────────┤
│ 模型层   大模型服务（可插拔）：DeepSeek / Qwen / GLM / Kimi   │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 技术选型

| 层级 | 选型 | 说明 |
|---|---|---|
| 前端 | Vue 3 + TypeScript + Ant Design Vue | 工作台 UI、文档预览（docx-preview）、批注组件 |
| 后端 | Python 3.12 + FastAPI | 异步 API、WebSocket 流式输出 |
| Agent 编排 | **LangGraph** | 状态机式编排，支持人工介入节点（interrupt）、条件分支、可恢复执行 |
| RAG 框架 | LlamaIndex（或 LangChain） | 文档加载、分块、索引、检索、重排 |
| 向量数据库 | Milvus（规模大） / pgvector（起步轻量） | 向量存储与相似检索 |
| 关系数据库 | PostgreSQL | 项目、用户、章节、批注、版本等业务数据 |
| 文档解析 | Unstructured + PyMuPDF + python-docx | PDF/Word 文本与表格抽取、版式识别 |
| 文档导出 | python-docx + Jinja2 模板 | 按标书模板渲染生成 .docx |
| LLM 网关 | LiteLLM（或自研 adapter） | 统一多模型接入，按任务路由、限流、降级 |
| 任务队列 | Redis + Celery / Arq | 长任务异步执行、进度上报 |
| 对象存储 | MinIO / 云 OSS | 原始文件与生成文档存储 |
| 部署 | Docker Compose（起步）→ Kubernetes（扩展） | 容器化、弹性伸缩 |
| 前端构建 | Vite | 快速开发与构建 |

**关键选型理由**：

- **LangGraph 而非裸 LangChain**：投标生成是"多步骤、可中断、需人工介入"的长流程，LangGraph 的图状态机天然支持「生成到某章节 → 暂停 → 人工批注 → 继续」，这是裸 Chain 难以实现的；
- **Milvus / pgvector 按规模选**：资料库 < 10 万文档块用 pgvector 即可（省运维），量大再切 Milvus；
- **LLM 网关统一路由**：解析类任务用高性价比模型（如 DeepSeek-V3），长文生成用上下文能力强的模型（如 Qwen-Long / GLM-4-Long），成本与质量平衡。

### 3.4 核心流程设计（时序）

```
用户            前端          后端/Agent编排        RAG服务           LLM          数据库
 │ 上传招标文件  → │             │                    │              │             │
 │               │  解析请求    → 解析节点            │              → 调用LLM      │
 │               │  解析结果    ← 结构化JSON ←─── (评分点/章节结构) ───┘            │
 │ 确认评分对标  → │  对标分析    → 对标节点 ──→ 生成评分对照表 ──→ 存库           │
 │               │  方案骨架    ← 章节树 + 评分映射                               │
 │ 逐节生成      → │  生成任务    → 章节生成节点 ──→ RAG检索(资料库) ──→ 撰写正文   │
 │   (流式展示)  ← │  章节内容流  ←──────────────────────────────┘               │
 │ 批注反馈      → │  批注事件    → 重写节点（仅局部） ──→ 生成新版本             │
 │ 导出Word      → │  导出请求    → 导出节点 ──→ 模板渲染 ──→ .docx 下载          │
```

---

## 四、核心功能详细设计

### 4.1 招标文件解析模块

**处理管线**：

1. **文件接入**：上传 PDF / Word → 对象存储 → 任务队列；
2. **文本抽取**：Unstructured 抽取正文与表格，保留章节层级（标题字号/编号识别）；PyMuPDF 处理扫描件时先 OCR（一期可选）；
3. **结构识别**：按常见招标文件结构（招标公告、投标人须知、评标办法、技术需求、合同条款）切分章节；
4. **要素抽取（LLM 结构化输出）**：
   - 评分点：`[{条款号, 评分项, 分值, 评审标准/细则, 是否为★实质性要求}]`
   - 资格要求：`[{类别(资质/业绩/人员/财务), 要求描述, 证明材料}]`
   - 技术需求清单：`[{序号, 需求描述, 类别(功能/性能/安全/实施/服务), 是否强制}]`
5. **人工确认**：解析结果以表格形式展示，投标专员可增删改后再进入下一步（避免错误要素带病传导）。

**评分点提取的 Prompt 设计要点**：提供评标办法章节原文 + 输出 JSON Schema 约束 + 要求标注"未明确分值不得臆测"，并二次校验（解析结果回读原文核对）。

### 4.2 评分对标分析模块

- **对标表结构**：条款号 / 评分项 / 分值 / 评审要点 / 我方应答章节 / 引用素材 / 应答策略 / 得分预估（高/中/低风险）；
- **风险分级**：按「分值 × 应答完备度」自动排序，高价值低覆盖项优先撰写；
- **失分点提示**：识别我方资料库中缺失的证明材料（如某资质未入库），提前预警售前补齐；
- 输出物：对标分析报告（Web 内联展示 + 可导出附表）。

### 4.3 RAG 知识检索模块

**入库流程**：

```
资料上传 → 分类识别(产品手册/案例/资质/专利) → 清洗 → 分块
   → 向量化(Embedding) → 向量库 + 元数据(PostgreSQL) → 双检索索引
```

- **分块策略**：按语义段落分块（500~800 字），产品参数表单独成块保留表格结构；每块携带元数据（文档名、类型、产品线、项目行业、时间）；
- **混合检索**：向量检索（语义）+ BM25 关键词检索（产品型号、资质编号等精确匹配），RRF 融合排序；
- **重排**：Reranker（如 bge-reranker）精排 Top-K，提升引用准确率；
- **引用溯源**：检索结果携带 chunk_id + 原文段落，生成内容标注 `【来源：XX案例 P12】`。

**资料库 Schema 设计（元数据）**：

```json
{
  "doc_id": "uuid",
  "doc_type": "product_manual | case_study | certificate | patent | template",
  "title": "XX平台技术白皮书",
  "product_line": "数据中台",
  "industry": "政务/金融/能源",
  "cert_no": "ISO27001-2023-xxxx",
  "upload_by": "售前-张三",
  "upload_at": "2026-08-01",
  "chunks": [{ "chunk_id": "...", "content": "...", "page_no": 12 }]
}
```

### 4.4 方案生成引擎（Agent 编排核心）

**生成流程（LangGraph 状态图）**：

```
[解析完成] → [生成骨架] → [逐章节循环: 检索→撰写→校验] → [全文整合] → [人工审阅] 
                              ↑_______________________________↓
                                    批注 → 局部重写（仅重写该节点）
```

1. **骨架生成节点**：输入评分点 + 技术需求 → 输出章节树 JSON（含章节标题、对应评分点、建议篇幅）；
2. **章节生成节点**：每章独立执行——
   - 检索节点：依据该章主题检索资料库（产品参数、案例、资质）；
   - 撰写节点：系统提示词 + 检索上下文 + 评分要求 → 生成正文（Markdown 中间格式）；
   - 校验节点：检查必答要点是否覆盖、是否与招标要求冲突（如参数低于★要求）、字数是否达标，不达标自动补写；
3. **人工介入节点（HITL）**：每章生成完成后状态暂停，用户可"通过 / 修改 / 批注重写"；用户确认后流程继续；
4. **全文整合**：统一术语、编号、目录层级，输出完整方案 Markdown 供预览。

**上下文管理**：采用「大纲记忆 + 局部上下文」策略——全局只保留章节树与关键决策摘要，各章节独立窗口生成，避免长文档上下文爆炸；每章生成时注入「前文摘要 + 本章评分要求 + 检索素材」。

### 4.5 人工审阅与迭代

- **审阅视图**：左侧章节树 + 右侧文档流，支持行内选中批注（批注类型：修改/补充/删除/存疑）；
- **局部重写**：批注收集后，仅将「该章节原文 + 批注列表」重新送入生成节点，产出新版本；
- **版本管理**：项目下维护版本链（V1 → V2 → …），支持任意两版本 diff 对比；
- **审阅效率**：支持按评分点过滤查看（"只看未覆盖评分点"）。

### 4.6 Word 文档导出

- **模板机制**：公司标书模板（.docx）作为母版，定义标题样式、页眉页脚、封面、目录域；
- **渲染流程**：方案 Markdown → 结构树 → python-docx 按模板样式写入 → 生成目录（Word 域，打开后自动更新）→ 分页/页码控制；
- **附表导出**：评分对照表、资质索引表（证件复印件清单）自动生成。

---

## 五、关键技术方案

### 5.1 长文档生成策略

| 问题 | 方案 |
|---|---|
| 上下文窗口有限 | 章节级生成：每章独立窗口，注入「大纲 + 前文摘要 + 本章要求」 |
| 前后不一致 | 术语表机制：骨架阶段生成术语表，各章统一引用；整合阶段一致性校验 |
| 生成中断/超时 | 章节为最小原子单位，逐章落库，支持断点续生成 |
| 质量不可控 | 校验节点：必答要点清单核查 + 字数/参数检查，失败自动重试（≤2 次） |

### 5.2 结构化输出（解析/骨架阶段）

- 统一采用 **JSON Schema 约束 + 函数调用（tool calling）**，避免自由文本解析；
- 解析结果二次校验：将提取的评分点回填原文位置，人工确认（FR1 要求）；
- 模型输出非法 JSON 时自动重试并提示修正。

### 5.3 Agent 编排（LangGraph 细节）

- **图定义**：节点（parse / skeleton / retrieve / write / validate / review / rewrite / export）+ 边（条件边、循环边）；
- **持久化**：Checkpointer（如 Postgres checkpointer）保存图状态，支撑任务中断恢复与人工介入暂停/续跑；
- **工具注册**：检索工具、导出工具、资料库查询工具、Web 检索工具（可选，需人工授权）；
- **流式输出**：章节内容逐 token 推送前端，用户实时可见。

### 5.4 提示词工程体系

| 提示词 | 用途 | 关键约束 |
|---|---|---|
| 招标解析器 | 评分点/资格/需求提取 | JSON Schema、禁止臆测分值、原文引用 |
| 骨架规划器 | 章节树生成 | 评分点全覆盖映射、标题符合标书惯例 |
| 章节撰写器 | 单节正文撰写 | 只可用检索素材、禁止编造参数、突出我方优势 |
| 批注重写器 | 按批注局部重写 | 仅修改批注涉及内容、保留其余原文 |
| 对标分析器 | 评分对标报告 | 分值权重排序、失分风险提示 |

提示词模板外置为配置文件（YAML），支持售前团队自行沉淀与调优。

### 5.5 检索质量优化

- Embedding 模型：bge-m3（中文能力强，支持 8k 长文本）为首选，可替换；
- 分块调优：表格块、段落块、清单块差异化切分；产品参数表优先保真；
- RRF 混合检索 + bge-reranker 精排；相似项目召回（按行业/产品线元数据过滤）；
- 检索评测集：沉淀 50~100 条"评分点 → 应引用素材"评测样本，回归验证检索效果。

### 5.6 并发与性能

- 长任务异步化：解析/生成/导出全部进任务队列（Redis + Arq），前端轮询/WebSocket 看进度；
- 生成并发：按模型限流（如 5 并发/模型），章节级排队执行；
- 缓存：解析结果、对标报告按项目缓存，重复生成不重复解析；
- 降级策略：模型超时/限流 → 切换备用模型；检索失败 → 降级为关键词搜索。

---

## 六、数据设计

### 6.1 核心数据表（PostgreSQL）

| 表名 | 说明 | 关键字段 |
|---|---|---|
| users / roles | 用户与角色 | 角色（投标专员/售前/审核人） |
| projects | 投标项目 | 项目名、招标编号、行业、状态、截止时间 |
| documents | 上传文档 | 类型（招标文件/资料/方案）、存储路径、解析状态 |
| score_points | 评分点 | 条款号、评分项、分值、评审标准、要求级别 |
| tech_requirements | 技术需求清单 | 序号、描述、类别、是否强制 |
| proposal_skeletons | 方案骨架 | 章节树 JSON、评分点映射 |
| proposal_sections | 方案章节 | 项目、章节号、内容 Markdown、状态、来源引用 |
| reviews | 审阅批注 | 章节、位置、批注类型、内容、处理状态 |
| proposal_versions | 方案版本 | 版本号、快照、变更说明、创建人 |
| kb_documents / kb_chunks | 资料库 | 元数据 + 分块内容（向量存 Milvus/pgvector） |

### 6.2 存储规划

| 存储 | 用途 | 容量估算（起步） |
|---|---|---|
| PostgreSQL | 业务数据 | 50GB（含向量扩展） |
| Milvus / pgvector | 向量 | 100 万条 chunk 以内 |
| MinIO / OSS | 原始文件 + 导出文档 | 200GB |
| Redis | 队列 + 缓存 | 4GB 内存 |

---

## 七、安全与合规

1. **数据隔离**：项目级权限控制，非本项目成员不可见；角色级操作权限（售前才能改资料库）；
2. **传输与存储加密**：HTTPS 全链路；敏感文档 AES-256 加密存储；
3. **LLM 调用合规**：外发大模型的提示词与素材需脱敏（自动去除手机号、身份证、客户敏感信息）；
4. **审计日志**：上传、生成、导出、批注全流程留痕；
5. **私有化选项**：如客户资料高度敏感，支持全部组件私有化部署（模型走私有化大模型或合规网关）；
6. **知识产权**：生成内容引用标注来源，历史方案复用遵守企业保密制度。

---

## 八、实施计划

### 8.1 里程碑

| 阶段 | 周期 | 交付内容 | 验收标准 |
|---|---|---|---|
| M1 基础平台 | 第 1~4 周 | Web 工作台、项目/文档管理、资料库上传入库、向量检索 | 资料库可检索，检索准确率 ≥ 80%（评测集） |
| M2 核心生成 | 第 5~10 周 | 招标解析、评分对标、骨架生成、章节生成（含 HITL） | 端到端跑通：上传招标文件 → 产出方案初稿 |
| M3 打磨迭代 | 第 11~14 周 | 批注重写、版本管理、Word 导出、流式体验优化 | 初稿质量达标（评分点覆盖 ≥ 95%），Word 格式合格 |
| M4 试点上线 | 第 15~16 周 | 试运行、检索评测集扩充、Prompt 调优、运维监控 | 3 个真实项目试点，平均出稿时间 ≤ 4 小时 |

### 8.2 团队配置（建议）

| 角色 | 人数 | 职责 |
|---|---|---|
| 项目经理 | 1 | 需求协调、进度管理 |
| 后端/Agent 工程师 | 2 | 编排引擎、RAG、API、队列 |
| 前端工程师 | 1 | 工作台、文档预览、批注 |
| AI 算法工程师 | 1 | 提示词工程、检索优化、评测 |
| 售前业务顾问 | 0.5 | 业务规则、标书模板、资料库整理 |

### 8.3 依赖项

- 大模型 API 账号（DeepSeek / 通义 / 智谱等，建议预留 2 家以上）；
- 公司标书模板、历史中标方案、产品资料（需售前团队配合整理入库）；
- 服务器资源：起步 8C16G × 2（CPU）+ GPU/高并发模型 API 预算。

---

## 九、风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|---|---|---|---|
| 生成内容幻觉（编造参数/案例） | 废标风险 | 中 | 强制引用溯源、校验节点比对 ★ 要求、关键数据人工确认 |
| 大模型输出不稳定 | 质量波动 | 中 | 章节原子化 + 校验重试、提示词版本化、评测集回归 |
| 招标文件格式刁钻（表格/扫描件） | 解析失败 | 中 | Unstructured 表格抽取 + 人工修正入口；OCR 二期补强 |
| 资料库素材不足 | 生成效果打折 | 中 | 试运行期售前集中入库；相似案例复用兜底 |
| 长文档生成耗时/成本高 | 体验与成本 | 低 | 章节级流式输出（首屏快）、模型按任务路由降本 |
| 数据安全合规 | 法律风险 | 低 | 脱敏、权限隔离、审计日志、私有化选项 |

---

## 十、附录

### 10.1 术语表

| 术语 | 说明 |
|---|---|
| HITL | Human-in-the-Loop，人工介入机制 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| RRF | Reciprocal Rank Fusion，混合检索结果融合排序 |
| 评分点 | 招标文件中评标办法中可量化的评审条目 |
| ★条款 | 招标文件中实质性（不可偏离）要求条款 |

### 10.2 下一步建议

1. **确认试点团队与首批资料清单**（3 个项目的历史方案 + 产品手册目录）；
2. **确定大模型供应商与预算**（建议 DeepSeek-V3 主用 + Qwen 备用）；
3. 本方案可进一步细化：前端原型设计、接口定义、数据库 DDL、LangGraph 状态图详细设计——需要时可继续展开。
