# 本地开发/测试覆盖（compose 自动合并）：
# - BID_LLM_MOCK=false：真实 LLM 调用（DeepSeek），供日常使用；
# - 跑 E2E 时请临时改回 "true"（E2E 前置 BID_LLM_MOCK=true）后重建容器。
services:
  api:
    environment:
      BID_LLM_MOCK: "false"
  worker:
    environment:
      BID_LLM_MOCK: "false"
